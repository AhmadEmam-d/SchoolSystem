You are a senior product designer and localization QA expert.

Your task is to fully audit, fix, and localize my entire project to a PERFECT Arabic RTL version.

Work across ALL pages, frames, components, variants, instances, hidden layers, and design system elements.

Apply these rules strictly:

1) SMART LANGUAGE DETECTION & AUTO-FIX:
- Detect any English text inside Arabic screens and translate it properly.
- Detect any Arabic text inside English screens and fix it according to the screen language.
- Ensure ZERO mixed-language UI.
- Maintain professional, natural Arabic wording.
- Fix placeholders, labels, tooltips, buttons, validation messages, table headers, empty states, notifications, and modals.
- If a component is reused in multiple places, fix it from the main component.

2) FULL RTL STRUCTURE CONVERSION (NOT TEXT ONLY):
- Convert entire layout direction to RTL.
- Mirror full layout horizontally.
- Reverse Auto Layout order correctly.
- Fix constraints to support RTL behavior.
- Move navigation, sidebars, breadcrumbs, back buttons to correct RTL positions.
- Ensure right text alignment everywhere.
- Mirror paddings, margins, and spacing logic.
- Keep layout balanced and pixel-perfect.

3) ICON & DIRECTION LOGIC:
- Flip directional icons (arrows, chevrons, sliders, pagination).
- Keep neutral icons unchanged.
- Ensure correct icon placement inside buttons and inputs.
- Fix progress steps direction.

4) COMPONENT SYSTEM PRIORITY:
- Start by fixing the Design System and main components first.
- Then propagate changes across all instances.
- Ensure consistency between variants.

5) TYPOGRAPHY & UI CONSISTENCY:
- Use proper Arabic typography.
- Fix line height and spacing if needed.
- Maintain hierarchy and visual clarity.

6) RESPONSIVE & BREAKPOINT CHECK:
- Ensure RTL works correctly in all screen sizes.
- Fix broken layouts after mirroring.

7) ZERO TOLERANCE MODE:
- Do not skip any frame.
- Do not leave any untranslated word.
- Do not keep mixed direction layouts.
- Do not redesign — only fix, localize, and mirror professionally.

Work screen by screen if necessary until the entire project is fully corrected.
If both English and Arabic versions exist, ensure each version strictly matches its correct language and layout direction automatically